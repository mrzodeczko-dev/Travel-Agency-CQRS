<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rzodeczko</groupId>
    <artifactId>travel-agency-cqrs-e2e</artifactId>
    <version>1.0.0</version>
    <name>Travel Agency CQRS — E2E Tests</name>
    <description>End-to-end tests for the full CQRS stack (run against live docker compose)</description>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.12.2</junit.version>
        <rest-assured.version>5.5.1</rest-assured.version>
        <awaitility.version>4.3.0</awaitility.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${rest-assured.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${awaitility.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.18.3</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.5</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mongodb</groupId>
            <artifactId>mongodb-driver-sync</artifactId>
            <version>5.4.0</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.3</version>
                <configuration>
                    <systemPropertyVariables>
                        <command.side.url>${env.COMMAND_SIDE_URL}</command.side.url>
                        <query.side.url>${env.QUERY_SIDE_URL}</query.side.url>
                        <e2e.propagation.timeout>${env.E2E_PROPAGATION_TIMEOUT}</e2e.propagation.timeout>
                        <db.pg.port>${env.TA_COMMAND_SIDE_SERVICE_DB_PORT}</db.pg.port>
                        <db.pg.name>${env.TA_COMMAND_SIDE_SERVICE_DB_NAME}</db.pg.name>
                        <db.pg.user>${env.TA_COMMAND_SIDE_SERVICE_DB_USER}</db.pg.user>
                        <db.pg.password>${env.TA_COMMAND_SIDE_SERVICE_DB_PASSWORD}</db.pg.password>
                        <db.mongo.port>${env.MONGO_PORT}</db.mongo.port>
                        <db.mongo.user>${env.MONGO_USER}</db.mongo.user>
                        <db.mongo.password>${env.MONGO_PASSWORD}</db.mongo.password>
                        <db.mongo.name>${env.MONGO_DB_NAME}</db.mongo.name>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
